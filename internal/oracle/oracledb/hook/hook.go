// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"

	"github.com/SethCurry/stax/internal/oracle/oracledb"
)

// The ArtistFunc type is an adapter to allow the use of ordinary
// function as Artist mutator.
type ArtistFunc func(context.Context, *oracledb.ArtistMutation) (oracledb.Value, error)

// Mutate calls f(ctx, m).
func (f ArtistFunc) Mutate(ctx context.Context, m oracledb.Mutation) (oracledb.Value, error) {
	if mv, ok := m.(*oracledb.ArtistMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *oracledb.ArtistMutation", m)
}

// The CardFunc type is an adapter to allow the use of ordinary
// function as Card mutator.
type CardFunc func(context.Context, *oracledb.CardMutation) (oracledb.Value, error)

// Mutate calls f(ctx, m).
func (f CardFunc) Mutate(ctx context.Context, m oracledb.Mutation) (oracledb.Value, error) {
	if mv, ok := m.(*oracledb.CardMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *oracledb.CardMutation", m)
}

// The CardFaceFunc type is an adapter to allow the use of ordinary
// function as CardFace mutator.
type CardFaceFunc func(context.Context, *oracledb.CardFaceMutation) (oracledb.Value, error)

// Mutate calls f(ctx, m).
func (f CardFaceFunc) Mutate(ctx context.Context, m oracledb.Mutation) (oracledb.Value, error) {
	if mv, ok := m.(*oracledb.CardFaceMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *oracledb.CardFaceMutation", m)
}

// The PrintingFunc type is an adapter to allow the use of ordinary
// function as Printing mutator.
type PrintingFunc func(context.Context, *oracledb.PrintingMutation) (oracledb.Value, error)

// Mutate calls f(ctx, m).
func (f PrintingFunc) Mutate(ctx context.Context, m oracledb.Mutation) (oracledb.Value, error) {
	if mv, ok := m.(*oracledb.PrintingMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *oracledb.PrintingMutation", m)
}

// The PrintingImageFunc type is an adapter to allow the use of ordinary
// function as PrintingImage mutator.
type PrintingImageFunc func(context.Context, *oracledb.PrintingImageMutation) (oracledb.Value, error)

// Mutate calls f(ctx, m).
func (f PrintingImageFunc) Mutate(ctx context.Context, m oracledb.Mutation) (oracledb.Value, error) {
	if mv, ok := m.(*oracledb.PrintingImageMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *oracledb.PrintingImageMutation", m)
}

// The RulingFunc type is an adapter to allow the use of ordinary
// function as Ruling mutator.
type RulingFunc func(context.Context, *oracledb.RulingMutation) (oracledb.Value, error)

// Mutate calls f(ctx, m).
func (f RulingFunc) Mutate(ctx context.Context, m oracledb.Mutation) (oracledb.Value, error) {
	if mv, ok := m.(*oracledb.RulingMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *oracledb.RulingMutation", m)
}

// The SetFunc type is an adapter to allow the use of ordinary
// function as Set mutator.
type SetFunc func(context.Context, *oracledb.SetMutation) (oracledb.Value, error)

// Mutate calls f(ctx, m).
func (f SetFunc) Mutate(ctx context.Context, m oracledb.Mutation) (oracledb.Value, error) {
	if mv, ok := m.(*oracledb.SetMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *oracledb.SetMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, oracledb.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m oracledb.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m oracledb.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m oracledb.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op oracledb.Op) Condition {
	return func(_ context.Context, m oracledb.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m oracledb.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m oracledb.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m oracledb.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk oracledb.Hook, cond Condition) oracledb.Hook {
	return func(next oracledb.Mutator) oracledb.Mutator {
		return oracledb.MutateFunc(func(ctx context.Context, m oracledb.Mutation) (oracledb.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, oracledb.Delete|oracledb.Create)
func On(hk oracledb.Hook, op oracledb.Op) oracledb.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, oracledb.Update|oracledb.UpdateOne)
func Unless(hk oracledb.Hook, op oracledb.Op) oracledb.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) oracledb.Hook {
	return func(oracledb.Mutator) oracledb.Mutator {
		return oracledb.MutateFunc(func(context.Context, oracledb.Mutation) (oracledb.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []oracledb.Hook {
//		return []oracledb.Hook{
//			Reject(oracledb.Delete|oracledb.Update),
//		}
//	}
func Reject(op oracledb.Op) oracledb.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []oracledb.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...oracledb.Hook) Chain {
	return Chain{append([]oracledb.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() oracledb.Hook {
	return func(mutator oracledb.Mutator) oracledb.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...oracledb.Hook) Chain {
	newHooks := make([]oracledb.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
